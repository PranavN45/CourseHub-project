Aaxios using for api fetching
chart js for charts

App be react router dom and sabke routes dena 

Stack is a div jiska display hai flex
space between me 1 is 4px ke barabar

in msp if u use the curly bracket then use the return statement.

chakra has its own css property like hiding a scroll bar 
css={{"&::-webkit-scrollbar":{
        display:"none"
      }}}

flexWrap = "wrap" 
wrap: With this value, the flex items will wrap to a new line when they no longer fit in the container's main axis direction. This means that if there's not enough space to fit all the items in a single row, they will move to the next row, creating a multi-line layout.

noOfLines mtlb ki agar 2 lines se jyada hoga toh ... likhke aayega

in reset password we need token aur woh we will access with the help of params
Tokens are often used for authentication and authorization purposes.
params.token token accessing

window.addEventListener("contextmenu", (e)=>{
  e.preventDefault();
})
- isse video ke window pe right click possible nahi hai

css={{
    cursor:`url(${cursor}), default`
  }}
  pure grid pe new cursor


function LinkButton(){
  return (
    <Link to={'/admin/dashboard'}>
        <Button  fontSize={"larger"} variant={"ghost"} >
          <RiDashboardFill style={{ margin: '4px' }} />
          Dashboard
        </Button>
      </Link>
    )
  }
  - ye ek function hai



cookie parser - req.cookies me se cookies accpet krne ke liye 
cors - server alag address pe frontend alag address pe , different host hone ke baad bhi api call hogi
jsonwebtoken - login ke liye jwt tokens chahiye
multer - req.files se files milengi
node-cron - ek fix time ya fir date pe woh function call hota rahega
validator - for email verification

password me select is for woh hume suggest na kare password


export const catchAsyncError = (passedFunction) => (req, res, next) => {
  Promise.resolve(passedFunction(req, res, next));
};
The purpose of this utility function is to catch any errors that might occur within the passedFunction and pass them to the Express error handling middleware 

agar koi middle ware nahi bacha hai toh error.js run hoga as we have selected next.

multer se file milegi usse uri lene ke liye datauri hai utils me 

express.json and express.urlencoded krna padta hai for req.body access

token ready json.sign({payload= jisko token karna hai})

update profile ke vakt password hash nahi krna hai

secure ko comment off krna baadme cuz we are workin on the localhost we need to use CORS

token ko reset krne ke liye we can use crypto


schema.methods.getResetToken = function () {
  const resetToken = crypto.randomBytes(20).toString("hex");

  this.resetPasswordToken = crypto
    .createHash("sha256")
    .update(resetToken)
    .digest("hex");

  return resetToken;
};

- the code generates a random reset token, hashes it, stores the hashed version in the database, and returns the original unhashed token to be sent to the user. The unhashed token is sent to the user so they can present it back to the application when requesting a password reset. The hashed token is stored in the database for comparison when the user later tries to use the token to reset their password.

Redux - ek aisa third party store jahase we can access the data. jahase we can access </App> </Home> </Post> sab access ho sakta hai. ye ek aisi library hai jahase state can get changed

for this we need 3 terminologies : Store, Reducer, Action, State
Store - jahape vairables vagera sab rahenge
Reducer - isse we can change the state of data intially if c=20 then we can change the state of c.
store ke andar ke values reducer change krega
()=>{ State.c=40 } Reducer se State ki value change kr sakte hai

Action - iske call krte hi reducer activate hoga like a trigger point.. Like ek button hai uspe agar apanne onClick function lagaya then usko dabate hi action call hoga hence reducer activate hoga

Store banaliya lekin usko app me add krna hota hai index.js me <App/> component hai usko react redux ke provider component ke sath wrap krdo 
ab Provider ko chahiye store

Hume chahiye useDispatch from react redux jisse we can trigger our action for the reducer vaala function
we have useSelector state me se value ko access krne ke liye
const {c} = useSelector(state=>state.custom) custom me se c chahiye OR 
const c = useSelector(state=>state.custom.c) dono chalte hai

cookie reload krte hi nikalni nahi chahiye isliye we need reducer of loadUser

protected-route-react - Protected routes are often used to enforce access control in your application. Not all users should have access to all parts of your app
razorpay ke build integration me ke script index.html me daaldena jisse we can accesss razorpay ke functions